{
  "name": "peon-pet",
  "version": "0.1.0",
  "description": "Desktop pet for Claude Code — an orc that reacts to peon-ping events",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev"
  },
  "dependencies": {
    "three": "^0.183.0"
  },
  "devDependencies": {
    "canvas": "^3.2.1",
    "electron": "^40.4.1"
  }
}
